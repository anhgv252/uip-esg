apiVersion: v1
kind: Service
metadata:
  name: {{ include "uip-analytics-service.fullname" . }}
  labels:
    app: {{ include "uip-analytics-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "uip-analytics-service.name" . }}
